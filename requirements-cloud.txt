# Cloud deployment requirements (Main API - lightweight)
# Core
flask==3.0.0
gunicorn==21.2.0
werkzeug==3.0.0
python-dotenv>=1.0.1
requests>=2.31.0

# Data processing
pandas==2.1.4
numpy==1.26.4

# Audio analysis
groq>=0.11.0
pydub>=0.25.1
httpx>=0.27.0

# Video/image processing (lightweight - no ML)
opencv-python-headless==4.9.0.80
pillow==10.2.0

# Note: py-feat runs on separate ML Worker service
# Main API calls ML Worker via HTTP for AU detection
