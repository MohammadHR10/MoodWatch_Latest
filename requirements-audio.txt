# Optional audio stack. Install only if you need audio diarization/analysis.
# Recommend Python 3.10 or 3.11 for best compatibility.
openai>=1.3.0
pyannote.audio>=3.1.1
# Choose a torch build compatible with your macOS/CPU/GPU.
# On Apple Silicon CPU-only (no Metal acceleration), a safe choice is:
# pip install torch==2.2.2 --index-url https://download.pytorch.org/whl/cpu
# Or omit pin and let pip resolve a CPU wheel.
torch
# Torchaudio aligned to torch version (pip will attempt a matching one)
torchaudio
soundfile>=0.12.1
# librosa brings numba/llvmlite; recent versions support Python 3.10/3.11 well
librosa>=0.10.1
